kind: pipeline
type: exec
name: kind_k8s

platform:
  os: darwin
  arch: amd64

steps:
- name: listing_clusters
  commands:
  - echo Hello, world
trigger:
  branch:
    include:
      - "CU-*"
      - master
  event:
  - push
  - pull_request